# Qatar 2022 Generated Reports

Active player-ranking release: `ranking-repair-v3.0-qatar-2022`.

- `ranking/player_rankings.csv` and `.json` are the complete feature/profile master data.
- `ranking/global_rankings_outfield.csv` and `ranking/player_rankings_300plus.*` are the current outfield leaderboards.
- `ranking/unified_tournament_rankings.csv` is the exact six-field outfield-only publication table.
- `ranking/goalkeeper_rankings.csv`, `.json`, and `.md` are the separate current goalkeeper ranking.
- `v3_figures/` contains the current outfield figures; `v5_figures/goalkeeper_rankings_v5.png` is the current goalkeeper figure.
- `canonical/model_summary.json` is the structured source for synchronized model-summary Markdown aliases.

Tournament Impact, Role Quality, and Uncertainty are separate products. Ordinary performance uses periods 1–4; period 5 is shootout-only.
